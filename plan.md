计划做一个实时查询潮汐的页面。
根据定位或者指定地点，当前时间，查找潮汐数据，显示潮汐图。
潮汐图，坐标形式，横轴时间，纵轴潮汐高度，有当前时间的时间线。
从哪儿可以得到潮汐数据，需要调查。
潮汐图要渲染的漂亮。
定位，根据手机定位，或者用户输入的地址，或者地图 选点定位。外接地图api用高德地图。

以上，
是不是已经说明的全部需求？还有哪些没有明确？


--- 做这些改进
1. 把机密信息，比如apikey等等，保存到.env文件，不要写死在代码里。
2. 每次请求得到的潮汐数据，保存到本地，不要每次都请求。下次请求的时候如果有数据，就用本地数据。保存格式你权衡判断。

---
1. 不要存储在浏览器端localstorage，保存在服务器端，保存为文件。数据复用性更高些。
2. 从现在的执行过程看，读取缓存的处理有问题，不过不重要了，改为服务器端保存。